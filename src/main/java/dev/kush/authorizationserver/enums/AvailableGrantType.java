package dev.kush.authorizationserver.enums;

public enum AvailableGrantType {
    authorization_code,
    client_credentials,
    refresh_token,
    password,
    device_code,
    jwt_bearer,
    token_exchange;
}
